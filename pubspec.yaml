name: flutter_lab
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  simple_logger:
  video_player:

  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  flutter_gen:

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/movies/
